repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: pytest tests/ -x -q --ignore=tests/integration
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
